[project]
name = "biodynformer-v1"
version = "0.1.0"
description = "Preictal-only BioDynFormer feature-bank and experiment runner."
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "scikit-learn",
  "torch",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
